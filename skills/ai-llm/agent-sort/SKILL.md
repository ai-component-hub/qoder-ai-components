---
name: 'agent-sort'
description: 'Build an evidence-backed ECC install plan for a specific repo by sorting'
adopted: 2026-05-16
batch: bulk-adoption
skills, commands, rules, hooks, and extras into DAILY vs LIBRARY buckets using parallel
repo-aware review passes. Use when ECC
source: everything-claude-code
---

# agent-sort

Build an evidence-backed ECC install plan for a specific repo by sorting skills, commands, rules, hooks, and extras into DAILY vs LIBRARY buckets using parallel repo-aware review passes. Use when ECC 

## When to Use

- Use this skill when working on tasks related to agent sort
- Apply best practices from everything-claude-code

## Workflow

name: agent-sort
description: Build an evidence-backed ECC install plan for a specific repo by sorting skills, commands, rules, hooks, and extras into DAILY vs LIBRARY buckets using parallel repo-aware review passes. Use when ECC should be trimmed to what a project actually needs instead of loading the full bundle.
origin: ECC
# Agent Sort
Use this skill when a repo needs a project-specific ECC surface instead of the default full install.
The goal is not to guess what "feels useful." The goal is to classify ECC components with evidence from the actual codebase.
## When to Use
- A project only 

## Source

Adopted from: everything-claude-code
